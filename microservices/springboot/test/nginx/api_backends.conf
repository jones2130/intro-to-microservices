upstream searchms {
    zone search_service 64k;
    server search:8080;
}

upstream inventoryms {
    zone inventory_service 64k;
    server inventory:8080;
}

upstream bookdatahandlerms {
    zone bookdatahandler_service 64k;
    server bookdatahandler:8080;
}